# Điền file này rồi dùng làm tham chiếu khi chạy các bước bên dưới.
# KHÔNG commit file đã điền mật khẩu thật (copy thành deploy/my.env.local nếu cần).

# --- VPS ---
VPS_HOST=123.456.789.0
VPS_USER=root
# SSH key: dùng file ~/.ssh/id_rsa hoặc paste vào GitHub Secret VPS_SSH_KEY

# --- App ---
APP_URL=http://123.456.789.0:8081
VPS_APP_DIR=/opt/apps/shinninghotel

# --- Database (phải trùng nhau ở compose + .env) ---
DB_DATABASE=shinninghotel_db
DB_USERNAME=shinninghotel_user
DB_PASSWORD=Thay_Mat_Khau_ManH_123
MYSQL_ROOT_PASSWORD=Thay_Root_ManH_456

# --- GitHub ---
GITHUB_REPO=PQT2802/shinninghotel-php8-mysql
GHCR_IMAGE=ghcr.io/pqt2802/shinninghotel-php8-mysql:latest
GITHUB_USER=PQT2802
